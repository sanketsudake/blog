# Reachability cycle — 2026-06-08

First cycle. A base-platform pass (UX + SEO + LLM-friendliness) before publishing new content, not a content-prioritization cycle.
No prior cycle to diff against.

## Signals

**Structural audit** (`docs/audit/2026-05-27.md`, 43 pages): 0 errors, 3 warnings —
2 over-long talk `<title>`s (`fission-soss-community-day` 85 chars, `fission-apache-kafka-pune` 68 chars) and 1 duplicate title ("Virtual Kubelet" on a tag page vs the talk).

**Analytics (GA4 + GSC):** not available this cycle — `GA4_PROPERTY_ID` is unset in `.env` and the gcloud ADC login lacks the `webmasters.readonly` scope (GSC returned 403). Repair handed to the user (see below). So this cycle ran on audit/structural signals only — appropriate for platform work, which is largely independent of per-page traffic.

**Qualitative gaps found** (codebase exploration): empty homepage meta description; generic homepage bio/headline; no `llms.txt`; Article JSON-LD missing `image`; no AI-crawler directives; RSS summary-only; no related-posts / post-to-post discovery; inconsistent post sign-offs (Twitter / LinkedIn / none); RSS feeds undiscoverable.

## Changes made

| Area | Change | Files |
|---|---|---|
| SEO | Homepage meta description (also feeds WebSite JSON-LD + OG) | `config/_default/params.toml` |
| SEO | Fixed 3 audit warnings (shortened 2 talk titles; retitled the Virtual Kubelet talk to disambiguate) | `content/talks/{fission-soss-community-day,fission-apache-kafka-pune}/index.md`, `content/talks/virtual-kubelet.md` |
| SEO | Enriched Article JSON-LD: `image`, `BlogPosting` for posts, `publisher`, URL-form `mainEntityOfPage` | `layouts/_partials/schema.html` (override) |
| SEO | Full-content RSS via `content:encoded`, gated to non-syndicated posts | `layouts/_default/rss.xml` (override) |
| LLM | `llms.txt` + `llms-full.txt` generated via custom output formats | `config/_default/hugo.toml`, `layouts/index.llms.txt`, `layouts/index.llmsfull.txt` |
| LLM | robots.txt explicitly allows GPTBot/ClaudeBot/PerplexityBot/Google-Extended/etc. + llms.txt hint | `layouts/robots.txt` |
| UX | Breadcrumbs, word count, inline tags, recentLimit 6 | `config/_default/params.toml` |
| UX | Related posts (Hugo `[related]` + partial + minimal single.html override) | `config/_default/hugo.toml`, `layouts/_partials/related.html`, `layouts/single.html` |
| UX | Stronger bio (role + domains + Fission hook; shows in homepage + article byline) and headline | `config/_default/languages.en.toml` |
| UX | Homepage intro + CTAs to Blog/Talks/About | `content/_index.md` (new) |
| UX | Footer menu surfacing RSS + GitHub + LinkedIn | `config/_default/menus.en.toml` |
| Content | Standardized post sign-offs to the canonical LinkedIn line | `content/posts/{coredns-kubernetes,kubeflow-kubernetes,virtual-kubelet,retrieval-augmented-generation-using-data-with-llms}/index.md` |
| Social | Regenerated 3 retitled talk OG cards (gradient fallback, no API cost) | `content/talks/*/feature.png`, `static/og/talks/virtual-kubelet.png` |
| Docs | Recorded the 2 Congo-partial overrides (re-diff on `hugo mod get -u`) + llms.txt notes | `CLAUDE.md` |

Verification: `hugo` builds clean; `site-audit.py --check` → **0 errors, 0 warnings** (was 3 warnings); `llms.txt`/`llms-full.txt` render unescaped; post JSON-LD validated as `BlogPosting` with image/publisher; only the original post carries `content:encoded`; related posts render relevant K8s posts.

## Hypotheses / what to re-measure next cycle

- **Allowing AI crawlers + llms.txt** → expect appearances/citations in LLM answer engines (hard to measure directly; watch referral traffic from perplexity/openai/claude in GA4).
- **Enriched JSON-LD (`image`, BlogPosting)** → eligibility for richer Google results; re-check Search Console "Rich results" / impressions on post URLs.
- **Related posts + footer RSS + homepage CTAs** → expect higher pages/session and lower bounce in GA4 once traffic data is available.
- **Fixed/sharper titles** → re-check CTR on the two retitled talks and any "kubernetes"-tag posts in GSC.

## Open follow-up (carry to next cycle)

**Analytics repair — DEFERRED this cycle.** The interactive auth was blocked: gcloud's default OAuth client is disallowed for the sensitive Analytics/Search Console scopes on this Google account ("This app is blocked"), and GA4/Search Console reject a service-account email as a user. Two paths remain for next time, either unblocks the report with no code change (the report reads ADC / `GOOGLE_APPLICATION_CREDENTIALS`):

- **Custom OAuth client (authenticate as yourself):** GCP Console → OAuth consent screen (External, add self as Test user) → Credentials → OAuth client ID (Desktop) → download JSON, then `gcloud auth application-default login --client-id-file=<json> --scopes=openid,email,https://www.googleapis.com/auth/cloud-platform,https://www.googleapis.com/auth/analytics.readonly,https://www.googleapis.com/auth/webmasters.readonly` → accept the "unverified app" warning → `set-quota-project`.
- **Service account (if org policy allows key creation AND the SA email is accepted in GA4/GSC):** create SA + JSON key, add the SA email as GA4 Viewer + Search Console user, then point the report at the key via `GOOGLE_APPLICATION_CREDENTIALS=<path>` in `.env` (key stored outside the repo).

Either way, finish by setting the numeric GA4 property id (Analytics Admin → Property Settings, *not* `G-EVHD7VH89H`) into `.env` as `GA4_PROPERTY_ID=...` and running `.venv/bin/python scripts/analytics-report.py` to confirm `docs/analytics/<date>.{md,json}` has data. `GSC_SITE_URL` is already set.

Owner is also exploring pulling the GA4/GSC numbers manually from the consoles in the meantime.

Once data flows, the **next** cycle becomes a true content-prioritization pass: fold GSC near-miss queries (position 5–20) and high-impression/low-CTR pages into title/summary rewrites and internal links.
